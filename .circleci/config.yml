version: 2
jobs:
  deploy:
    docker:
      - image: eu.gcr.io/ricardo-dev-ch/platform/isopod:v7
        auth:
          username: _json_key
          password: $GCLOUD_SERVICE_KEY
    steps:
      - add_ssh_keys:
          fingerprints:
            - "db:02:b9:24:9d:da:2d:10:d1:f4:25:33:28:de:f2:60"
      - checkout
      - run: isopod setup
      - run: isopod ci-setup-access
      - run: isopod build
      - run: isopod deploy
      # Debug commands
      # - run: isopod info
      # - run: kubectl config view
      # - run: kubectl get nodes
      # - run: cat skaffold.yaml
      # Manual image build
      # - run: gcloud builds submit --substitutions=SHORT_SHA="${CIRCLE_SHA1::7}" .

  # job-two:
  #   docker:
  #     # - image: circleci/ruby:2.4.1
  #     - image: ruby:2.6.3
  #   steps:
  #     - checkout
  #     - run: echo "A more familiar hi"
  #     - run: ls -la
  #     - run: pwd
  #     - run: ls -l ~/
  #     - run: ls -l ~/.ssh

workflows:
  version: 2
  one_and_two:
    jobs:
      - deploy:
          context: dev-cookie
      # - job-two:
      #     requires:
      #       - job-one
